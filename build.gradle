buildscript {
  repositories {
    mavenCentral()
  }
  dependencies {
  }
}

plugins {
  id("io.spinnaker.plugin.bundler").version("$spinnakerGradleVersion")
  id("com.palantir.git-version").version("0.12.2")
  id("com.moowork.node").version("1.3.1").apply(false)
}

spinnakerBundle {
  pluginId = "ko28.DefaultRolePlugin"
  description = "Adds the fiat role 'public' to all users."
  provider = "https://github.com/ko28"
  //version = rootProject.version
  version = projectVersion

}

//version = normalizedVersion()

subprojects {
  group = "io.ko28.plugin.spring"
  //version = rootProject.version
  version = projectVersion
}

//String normalizedVersion() {
//  String fullVersion = gitVersion()
//  String normalized = fullVersion.split("-").first()
//  if (fullVersion.contains("dirty")) {
//    return "$normalized-SNAPSHOT"
//  } else {
//    return normalized
//  }
//}
